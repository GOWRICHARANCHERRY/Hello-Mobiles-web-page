name: Keep Render Awake

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping hello-mobiles.com
        run: curl -s -o /dev/null -w "status:%{http_code}\n" --max-time 60 https://hello-mobiles.com
